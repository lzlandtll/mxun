server:
  port: 7006

spring:
  application:
    name: mxun-mem
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.50.138:8848
  config:
    import:
      - classpath:mybatis-flex-config.yml
      - classpath:redis.yml
      - classpath:kafka.yml
