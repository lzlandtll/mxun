server:
  port: 7005

spring:
  application:
    name: mxun-art
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.50.138:8848
  config:
    import:
      - classpath:mybatis-flex-config.yml
      - classpath:redis.yml
      - classpath:elasticsearch.yml
