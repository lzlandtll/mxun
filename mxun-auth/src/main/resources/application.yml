server:
  port: 7002
spring:
  application:
    name: mxun-auth
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  config:
    import:
      - classpath:mybatis-flex-config.yml
      - classpath:redis.yml